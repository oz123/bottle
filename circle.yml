dependencies:
  cache_directories:
    - .pip-cache
    - .pyenv

test:
  override:
    - pyenv versions
    - pyenv shell 2.7.3 
    - eval "$(pyenv init -)"
    - python --version; python test/testall.py
   
   

